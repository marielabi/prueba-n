










/*document.addEventListener('DOMContentLoaded', function() {
    // Recuperar las preferencias almacenadas en localStorage
    const preferences = JSON.parse(localStorage.getItem('preferences'));

    // Mostrar el producto personalizado en el carrito
    if (preferences) {
        document.getElementById('sizeDisplay').textContent = preferences.size;
        document.getElementById('quantityDisplay').textContent = preferences.quantity;
        document.getElementById('colorDisplay').textContent = preferences.color;
        document.getElementById('textDisplay').textContent = preferences.text;
    }
});*/
